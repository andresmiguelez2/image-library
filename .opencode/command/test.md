---
description: Run the test suite with pytest.
---

Run the tests:

```
uv run pytest -q
```

Report pass/fail counts and fix any failures you introduced before
summarizing. If there are no tests yet, say so and suggest what to cover
(scanner hash-skip logic, EXIF extraction, catalog filters).